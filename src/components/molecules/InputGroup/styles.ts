import styled from "styled-components"

export const InputGroupContainer = styled.div``
export const InputGroupErrorMessage = styled.span`
  color: ${({ theme }) => theme.COLORS.RED[500]};
  font-size: 14px;
`
