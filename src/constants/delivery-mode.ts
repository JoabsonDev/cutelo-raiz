import { t } from "i18next"

const DeliveryMode = {
  Delivery: t("deliveryMode.delivery"),
  TakeAway: t("deliveryMode.takeAway"),
  InStore: t("deliveryMode.inStore"),
}

export default DeliveryMode
