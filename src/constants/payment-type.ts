import { t } from "i18next"

const PaymentType = {
  CreditCard: `${t("paymentMethod.paymentType.creditCard")}`,
  DebitCard: `${t("paymentMethod.paymentType.debitCard")}`,
  Money: `${t("paymentMethod.paymentType.money")}`,
  Pix: `${t("paymentMethod.paymentType.pix")}`,
}

export default PaymentType
